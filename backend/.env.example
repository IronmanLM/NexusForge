NODE_ENV=production
PORT=4000
ENABLE_DEMO_SEED=false

# Backend persistent state (JSON file)
DATA_DIR=./data
DATA_FILE=./data/state.json

# CORS frontend URL
CORS_ORIGIN=https://nexusforge.en-ligne.fr

# Public URLs used in email links
APP_BASE_URL=https://nexusforge.en-ligne.fr
API_BASE_URL=https://api.nexusforge.en-ligne.fr

# Shared secret used by the Discord bot to read backend events
DISCORD_BOT_SHARED_SECRET=change-me-discord-bot-secret

# Shared secret used to trigger production backups through the internal URL
BACKUP_TRIGGER_SECRET=change-me-backup-secret
BACKUP_REMOTE_HOST=fremaux.biz
BACKUP_REMOTE_USER=root
BACKUP_REMOTE_DIR=/mnt/kraken/Backups/nexusforge_backups
BACKUP_REMOTE_HISTORY_DIR=/mnt/kraken/Backups/nexusforge_backups/history
BACKUP_SSH_KEY=/home/mcxk1700/.ssh/id_ed25519_nexusforge_backup

# Discord OAuth2 account linking
DISCORD_OAUTH_CLIENT_ID=
DISCORD_OAUTH_CLIENT_SECRET=
DISCORD_OAUTH_REDIRECT_URI=https://nexusforge.en-ligne.fr/auth/discord/callback

# JWT (required in production, set long random secrets)
JWT_SECRET=change-me-access-secret
JWT_REFRESH_SECRET=change-me-refresh-secret
ACCESS_TOKEN_EXPIRES_IN=1h
REFRESH_TOKEN_EXPIRES_IN=30d

# Root admin (protected account)
# Required the first time the production root admin is created.
# Once the account exists, the persisted password hash is kept unless you set a new value here.
ROOT_ADMIN_FIRST_NAME=Mikael
ROOT_ADMIN_LAST_NAME=Fremaux
ROOT_ADMIN_NICKNAME=IronmanLM
ROOT_ADMIN_EMAIL=ironmanlm@en-ligne.fr
ROOT_ADMIN_PASSWORD=change-me-root-admin-password
# Optional: force 2FA TOTP for protected root admin (BASE32 secret, no spaces)
ROOT_ADMIN_TOTP_SECRET=

# Account lockout policy (after MAX_FAILED_ATTEMPTS: 15m, 30m, 1h)
MAX_FAILED_ATTEMPTS=5

# Token TTLs (milliseconds)
EMAIL_TOKEN_TTL_MS=86400000
RESET_TOKEN_TTL_MS=3600000
TWO_FACTOR_CHALLENGE_TTL_MS=300000

# SMTP (o2switch)
SMTP_HOST=mail.votre-domaine.tld
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=no-reply@votre-domaine.tld
SMTP_PASS=change-me-smtp-password
SMTP_FROM="NexusForge <no-reply@votre-domaine.tld>"
